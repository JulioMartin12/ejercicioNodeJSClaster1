const libraryService = require('./library');
const bookService = require('./book');
const userService = require('./user');
const authService = require('./auth');

module.exports = {libraryService, bookService, userService, authService};